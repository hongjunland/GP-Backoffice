server:
  port: 8081

spring:
  application:
    name: attendance-management-service
  profiles:
    active: dev
  datasource:
    url: jdbc:mariadb://localhost:3306/attendance-management-db
    username: root
    password: taeyun1215

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
